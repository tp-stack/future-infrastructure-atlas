# Layer Catalog

The initial layer catalog is defined in `config/layers.yaml`.

Layer definitions include:

- layer ID
- display name
- geometry type
- category
- zoom range
- sensitivity level
- default visibility
- allowed precision
- required and optional fields

Sensitive layers are configured for generalized precision and conservative default visibility until a later review establishes safe publication rules.
