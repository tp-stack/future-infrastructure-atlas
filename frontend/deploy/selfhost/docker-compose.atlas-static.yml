services:
  atlas-static:
    image: caddy:2-alpine
    container_name: atlas-static
    restart: unless-stopped
    ports:
      - "127.0.0.1:3460:3460"
    volumes:
      - ../../dist:/srv:ro
      - ./atlas-static/Caddyfile:/etc/caddy/Caddyfile:ro
