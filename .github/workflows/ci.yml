name: CI

on:
  push:
    branches: [master]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Set up Chrome
        uses: browser-actions/setup-chrome@v1

      - name: Install Python dependencies
        run: python -m pip install -e .

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Validate storage and frontend data
        run: |
          python scripts/init_storage.py
          python scripts/check_registry.py
          python scripts/check_frontend_data.py
          python -m atlas.storage .

      - name: Build frontend
        working-directory: frontend
        run: npm run build

      - name: Visual regression guard
        run: python scripts/check_visual_regression.py

      - name: Python tests
        run: pytest -q --ignore=tests/test_visual_regression.py

      - name: Upload visual regression screenshots on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: visual-regression-screenshots
          path: data/reports/visual_regression
          if-no-files-found: ignore
